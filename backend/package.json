{
  "name": "my-sync-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd ../frontend && npm install && npx vite build && mkdir -p ../backend/frontend/dist && cp -r ../frontend/dist/* ../backend/frontend/dist/",
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "argon2": "^0.40.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "socket.io": "^4.7.2",
    "uuid": "^9.0.1"
  }
}